# Пример переменных окружения (скопируйте в .env.local и поправьте под себя).
# Секретов здесь нет: значения попадают в собранный бандл.

# URL API бота (Cloudflare Worker). Не задано — используется прод.
VITE_BOT_API=https://book-club-bot.vitrumbeta.workers.dev
